/// Link to the app, appended when a user shares a verse — update this once
/// the app is live on the App Store / Play Store (or has a marketing page).
/// Left empty means the share text omits the link entirely.
const String appShareLink = '';
